<div class="category edit-page">
  <div class="card">
    <div class="card-body">
      <h2><%= t('common.editig_category') %></h2>
      <%= render "form", category: @category%>
      <%= link_to t('common.show'), @category, class: 'btn-interaction' %> |
      <%= link_to t('common.back'), categories_path, class: 'btn-interaction' %>
    </div>
    </div>
  </div>
</div>
