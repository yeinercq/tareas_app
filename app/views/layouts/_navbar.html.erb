<nav class="navbar navbar-expand-lg navbar-dark bg-dark-blue">
  <div class="container-fluid">
    <a class="navbar-brand">
      <%= image_tag 'navBarLogo.png', class: 'd-inline-block align-top', width: 45 %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item active">
          <a class="nav-link" href="<%= root_path %>">Inicio</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= categories_path %>">Categorias</a>
        </li>
      </ul>
    </div>
    <%= link_to 'Crear Tarea', new_task_path, class: 'btn btn-light ml-2' %>
    <div class="dropdown dropdown-user">
      <a class="dropdown-toggle" id="userMenu" data-bs-toggle="dropdown" aria-expanded="false">
        <%= image_tag "https://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(current_user.email)}?size=38x38" %>
      </a>
      <div class="dropdown-menu dropdown-menu-end" aria-labelledby="userMenu">
        <div class="dropdown-item small text-right">
          <%= current_user.email %>
        </div>
        <hr class="dropdown-divider">
        <%= link_to destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: 'dropdown-item' do %>
          <div class="fas fas-sign-out"></div>
          Cerrar sesion
        <% end %>
      </div>
    </div>
  </div>
</nav>
